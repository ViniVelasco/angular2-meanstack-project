var User = /** @class */ (function () {
    function User(email, password, firstName, // optional
    // optional
    lastName) {
        this.email = email;
        this.password = password;
        this.firstName = firstName;
        this.lastName = lastName;
    }
    return User;
}());
export { User };
